import React from 'react'

/**
 * DashIntegration is a hack to present an iframe for the Dash
 * @returns 
 */
const DashIntegration = () => {

    return(<>
    <h2>Dashboard</h2>
    Below is the Dashboard. Loading may take time...
    <iframe src="" />
    </>)
}